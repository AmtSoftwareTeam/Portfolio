import { Variants } from "framer-motion";

const variantsText: Variants = {
  animate: {
    opacity: 1,
    transition: {
      delay: 0.3,
      duration: 0.7,
      delayChildren: 1,
      staggerChildren: 0.05,
    },
  },
};

export { variantsText };
